@model MainBit.Alias.Models.UrlTemplateRecord
@{
    Layout.Title = T("Update URL template");
}

@using (Html.BeginFormAntiForgeryPost())
{
    @Html.ValidationSummary()
    @Html.HiddenFor(m => m.Id)
    <fieldset>
        @Html.LabelFor(m => m.Position, T("Position"))
        @Html.TextBoxFor(m => m.Position, new { type = "number", @class = "text small" })
        @Html.ValidationMessageFor(m => m.Position)
        <span class="hint">@T("")</span>
    </fieldset>
    <fieldset>
        @Html.LabelFor(m => m.BaseUrl, T("Base url"))
        @Html.TextBoxFor(m => m.BaseUrl, new { @class = "text large" })
        @Html.ValidationMessageFor(m => m.BaseUrl)
        <span class="hint">@T("")</span>
    </fieldset>
    <fieldset>
        @Html.LabelFor(m => m.StoredVirtualPath, T("Stored virtual path"))
        @Html.TextBoxFor(m => m.StoredVirtualPath, new { @class = "text large" })
        @Html.ValidationMessageFor(m => m.StoredVirtualPath)
        <span class="hint">@T("")</span>
    </fieldset>
    <fieldset>
        <button class="primaryAction" type="submit">@T("Save")</button>
        @Html.ActionLink(T("Cancel").ToString(), "Index", new { }, new { @class = "button" })
    </fieldset>
}